# Bitácora de las pruebas de tecnología

> **REGISTRO DE IA · NO ES FUENTE**
> Generado por: Claude · 14-sep-2026
> Estado: **SIN REVISAR**
> Manda sobre esto: `ADR-PoC-Spikes.xlsx` · `DRIVERS_ARQUITECTONICOS.md`

**Append-only.** Cada chat añade su línea al final cuando termina. **Nunca se edita lo anterior**;
si algo salió mal se añade una línea nueva que lo corrija.

Formato: `| fecha | spike | chat | qué se probó | qué salió | dónde quedó |`

| Fecha | Spike | Chat | Qué se probó | Qué salió | Dónde quedó |
|---|---|---|---|---|---|
| 2026-09-14 | — | 14-sep · plan de tecnologías | Se creó la estructura de `spikes/` y una primera terna (`SPK-09`/`10`/`11`) | Sin medir nada | Esta carpeta |
| 2026-09-14 | — | 14-sep · plan de tecnologías | **Reenfoque a solo Backend de la finca.** Terna nueva: `SPK-09` motor de reglas · `SPK-12` ingreso y sincronización · `SPK-13` cifrado y respaldo. `SPK-10` y `SPK-11` aparcados | Sin medir nada | `IA_CONTEXTO-3-BACKEND.md` y las tres carpetas |
| 2026-09-15 | — | 15-sep · plan de tecnologías | Segunda tanda de spikes: `SPK-14` paquete de configuración · `SPK-15` empaquetado e instalación · `SPK-16` salida de datos al BI | Sin medir nada | Las tres carpetas nuevas |
| 2026-09-15 | `SPK-14` | 4 · paquete de configuración | El paquete único firmado en tres formas (partido · personalizado · partido + lista de revocación), la compuerta de `ADR-015` en dos lenguajes, reproducibilidad de un veredicto de marzo, seis ataques de integridad, bajada completa contra incremental, y sensibilidad al tamaño de la finca | El tamaño no decide: 66 KB, bajada en centésimas. Decide la revocación — 232 B contra 665 KB, 15 ms contra 14 s. Personalizar enfrenta el número único de `ADR-029` con `ESC-22`. Y la huella del veredicto depende también de la forma canónica del JSON, hoy sin fijar | `SPK-14-paquete-de-configuracion/IA_RESULTADO.md` · `labs/spk-14/` |
| 2026-09-15 | `SPK-16` | 6 · salida de datos al BI | Extracción de un año por tres caminos (rol de lectura, REST propio, OData), en dos lenguajes; acotamiento por empresa, salto de interfaz, refresco concurrente y corrida sin internet | `ESC-29` sobra: peor caso 68 s de 600. **La RLS no protege las vistas materializadas: 239.200 filas de otra empresa visibles hasta aplicar el arreglo.** Regenerar el derivado encola a los lectores que llegan: 19,92 s a la primera fila, 0,03 s con `CONCURRENTLY`. python y node dentro del 4 % | `SPK-16-salida-de-datos-bi/IA_RESULTADO.md` · `labs/spk-16/` |
| 2026-09-15 | `SPK-15` | 5 · empaquetado, instalación y actualización | Instalación en limpio por fases en tres lenguajes, cadena de 40 migraciones sobre 20 empresas de 250.000 eventos, cortes a mitad de cadena y de parque, actualización con ingesta y cola vivas, vuelta atrás de código y de esquema, paridad del artefacto construido dos veces, divergencia de `CN-29` y huella real del nodo | Instalar el software son 2,5 s y actualizar 20 empresas 3,7 min: el lenguaje mueve entre el 3 % y el 36 %. **No se pierde dato al actualizar en caliente —0 eventos, cola intacta— pero la aplicación vieja muere: 28 de 40 s sin poder escribir.** Una consulta de 20 s convierte un `ALTER` de 33 ms en 18 s de escrituras paradas; con `lock_timeout`, 2 s. 39 versiones de atraso se recuperan enteras y dejan el esquema idéntico al de una instalación nueva: **lo que mata a la instalación atrasada es aplanar la cadena, no la deuda**. El disco sube 77 % al actualizar y no baja solo | `SPK-15-empaquetado-e-instalacion/IA_RESULTADO.md` · `labs/spk-15/` |
| 2026-09-15 | `SPK-12` | 15-sep · chat 2 de 3, cierre | **Aplazado por decisión de Juan**, no por falta de medición: el spike está medido pero el territorio sigue demasiado contradictorio para definir | Cabecera a `APLAZADO 15-sep-2026`, movido a `Aparcados` del `LEEME`, y punto de reentrada escrito como `§0` del propio registro: los bloqueos ordenados (`D1` es la raíz), los cuatro resultados que **no** hay que volver a medir, y qué medir primero al retomar | `SPK-12-ingreso-y-sincronizacion/IA_RESULTADO.md` §0 · `labs/spk-12/` intacto |
| 2026-09-15 | `SPK-09`..`SPK-16` | 15-sep · consolidación | **No se midió nada nuevo.** Se destilaron los seis `IA_RESULTADO.md` (09, 12, 13, 14, 15, 16) a una fila por spike y se acumularon en un solo libro, al estilo del libro maestro de ADR. Se marcaron los spikes abiertos imprescindibles y se separó lo que ya no se mide sino que se decide | **Cuatro spikes abiertos en rojo:** `AB-01` validador del paquete (forma canónica del JSON + tipo de regla desconocido) · `AB-02` prueba automática del cero de `CT-05` sobre el modelo derivado · `AB-03` LUKS de verdad en el nodo real · `AB-04` rotación de llaves contra los 5 años de `ADR-022`. Cinco más en ámbar y 25 decisiones pendientes ordenadas. Los seis `IA_RESULTADO.md` quedan marcados para erradicar; `labs/` no se toca | `IA_SPIKES-resumen.xlsx` (esta carpeta) · resumen de lectura en `_to_delete/resumenes-de-lectura/` |
| 2026-09-15 | `SPK-17` | 15-sep · apertura | **Nada medido todavía.** Se abrió `SPK-17` para cerrar el único hueco de lenguaje que queda: Java y C# no tienen ni una cifra, porque `SPK-09` los dejó fuera por falta de cadena de herramientas y `SPK-15` midió la instalación solo en Node, Python y Go. Hoy se descartarían **por ausencia de datos** | Pregunta lista con ocho mediciones, la primera como compuerta (¿existe el toolchain?): huella canónica del JSON (cierra `AB-01` en dos lenguajes más) · camino A2 a wasm, que hoy vive en un rango de 4.000× · batería de 1.232 camas de `SPK-09` · instalación y huella contra `SPK-15`, **más si el nodo necesita JRE o runtime de .NET** · las tres banderas de paridad · el migrador · la cadena de conexión · el costo para dos personas. **No elige ganador y no escribe ADR** | `SPK-17-java-y-csharp/IA_PREGUNTA.md` · `labs/spk-17/` cuando arranque |
